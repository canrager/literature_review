# Learnings

## VS Code: Markdown Preview Enhanced — Multiple Previews

Set `markdown-preview-enhanced.previewMode` to **"Multiple Previews"** in VS Code settings. Each markdown file then gets its own persistent preview pane that doesn't auto-switch when you click another file.
